name: Keep Streamlit Alive

on:
  schedule:
    - cron: "0 */10 * * *"   # runs every 10 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit App
        run: |
          curl -L https://aihoneypotanalytics-hlocqps4ww3cai38nzjhon.streamlit.app/
